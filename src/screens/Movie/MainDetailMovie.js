import {
    Text, View,
    TouchableOpacity,
    ImageBackground,
    Image,
} from 'react-native'

import MainLayouts from '../../layouts/MainLayouts'

export default MainDetailMovie = ({ navigation }) => {
    return(
        <>
            <MainLayouts navigation={ navigation }>
                <View>
                    <Text>Tester Here For Detail Movie</Text>
                </View>
            </MainLayouts>
        </>
    )
}